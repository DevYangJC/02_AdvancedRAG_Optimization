# 2026-08-13 工作日志

## Ragas 评估体系源码分析 + 技术文章
- 通读了项目 Ragas 评估相关源码，产出一篇技术文章 `docs/ragas评估体系详解.md`。
- 关键发现：
  - Ragas 版本 0.4.3，三个入口：`eval_service.py`(Web)、`eval_ragas.py`(脚本)、`generate_testset.py`(合成测试集)。
  - 四个指标：faithfulness / answer_relevancy / context_precision / context_recall。
  - 复用项目方法：`retrieve`、`rerank`、`build_context`、`build_chat_pipeline`、`get_cached_embedder`、`settings`。
  - **重要坑**：评估管线是简化版（retrieve top_k=20 硬编码、rerank top_n=6 硬编码、无多样性采样/去重），与生产管线（top_k=100 → 多样性采样 → rerank top_n=12 → 去重）不一致。
- 项目约定：评估裁判模型用 qwen-max，生成模型用 qwen-plus；temperature 裁判 0.0、生成 0.2。

## 状态变更：用户删除了离线评估脚本
- 用户删除了 `backend/scripts/eval_ragas.py`、`generate_testset.py`、`seed_qdrant.py`（均为 untracked，git 无备份）。
- 评估入口现只剩 Web 服务端 `eval_service.py`（前端评估中心在用）。
- "评估时拿重排后片段"的代码位置：`eval_service.py` 第 155-169 行（retrieve top_k=20 → 截断300 → rerank top_n=6 → `reranked_hits = [hits[idx_r] for idx_r,_ in ranked]` → 提取 retrieved_contexts）。

## 重写评估文档（以数据流转为主线）
- 按用户要求重写 `docs/ragas评估体系详解.md`：主线改为"每个阶段的数据怎么喂给 Ragas"，配完整代码+注释。
- 核心字段映射（Ragas 只认四个字段）：`user_input←question`、`response←answer_text`、`retrieved_contexts←contexts`、`reference←ground_truth`。
- 四个指标吃哪些字段：faithfulness(response+contexts)、answer_relevancy(user_input+response)、context_precision/recall(user_input+reference+contexts)。
- 追加"CSV 报告解读"章节：8 列分两部分（前4输入字段+后4评分字段），并纳入真实案例（第3条数据：连接池问题检索全是简历噪音 → faithfulness 0.125 / context_precision 0.0 / context_recall 0.0 / answer_relevancy 0.939）。真实报告文件 `backend/ragas_eval_report.csv`。

## 优化 RAG 评估页面功能（前后端）
- 注意：eval_service.py / evaluation.py / schemas 此前被用户重构过，函数名是 `create_and_run_task`/`get_task`/`list_tasks`/`get_task_records`，schema 是 `EvalTaskOut`/`EvalRecordOut`。**以文件系统当前状态为准**，早期读的旧内容（带分页 list_eval_tasks/download_report/status 接口/report_service）已不存在。
- 后端改动：
  - `models/evaluation.py`：EvalTask 加 `review` 字段（Text, nullable）。
  - `db/session.py`：init_db 里手动 ALTER TABLE 补 eval_tasks.review 列（create_all 不改已有表；项目原本没有迁移逻辑）。
  - `schemas/evaluation.py`：EvalTaskOut 加 review 字段。
  - `eval_service.py`：新增 `generate_eval_review()`（用 llm_service.ainvoke 生成"最低指标+优化建议"）、`delete_task()`、`regenerate_review()`；评估 done 时自动生成 review。
  - `api/v1/evaluation.py`：新增 DELETE /tasks/{id}、POST /tasks/{id}/review。
- 前端改动：`api/evaluation.ts` 加 deleteEvalTask/regenerateEvalReview + EvalTask.review 类型；`views/EvaluationView.vue` 历史记录加删除按钮（ElMessageBox 确认）、结果面板加"AI 评估建议"区（pre-wrap 展示 review + 重新生成按钮）。
- 验证：后端 py_compile 通过；前端 vue-tsc -b 类型检查通过。
